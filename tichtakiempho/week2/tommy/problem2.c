#include <stdio.h> 
#include <math.h>

int main() {
	double x, y;
	printf("so 1: \n");
	scanf("%lf", &x);
	printf("so 2: \n");
	scanf("%lf", &y);
	double s3 = pow(x,y);
	printf("ket qua la: %lf", s3);
}
